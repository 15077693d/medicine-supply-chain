import React from 'react';
import  {Blocker} from '../globalStyles'
const CreateChain = () => {
    return (
        <Blocker>
          This is a blocker  
        </Blocker>
    );
};

export default CreateChain;